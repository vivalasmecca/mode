{
  "buildCommand": "mkdir -p mode-data && cp -r ../output mode-data/output && cp -r ../tokens mode-data/tokens && cp -r ../context mode-data/context && cp -r ../manifest mode-data/manifest && (cp -r ../config mode-data/config 2>/dev/null || true) && npm run build",
  "framework": "nextjs"
}
